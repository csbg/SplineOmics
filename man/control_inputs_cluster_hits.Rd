% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_hits.R
\name{control_inputs_cluster_hits}
\alias{control_inputs_cluster_hits}
\title{Control Inputs for Cluster Hits}
\usage{
control_inputs_cluster_hits(
  top_tables,
  data,
  meta,
  mode,
  spline_params,
  condition,
  p_values,
  clusters,
  report_info,
  meta_batch_column,
  time_unit,
  report_dir
)
}
\arguments{
\item{top_tables}{A list of top tables from limma analysis.}

\item{data}{A matrix of data values.}

\item{meta}{A dataframe containing metadata.}

\item{mode}{A character string specifying the mode ('isolated' or 'integrated').}

\item{spline_params}{A list of spline parameters for the analysis.}

\item{condition}{A character string specifying the condition.}

\item{p_values}{A numeric vector of p-values.}

\item{clusters}{A list specifying clusters or "auto" for automatic estimation.}

\item{report_info}{An object containing report information.}

\item{meta_batch_column}{A character string specifying the meta batch column.}

\item{time_unit}{A character string specifying the time unit ('s', 'm', 'h', 'd').}

\item{report_dir}{A character string specifying the report directory.}
}
\value{
No return value, called for side effects.
}
\description{
Validates the inputs for clustering hits, ensuring all required structures 
and parameters are correctly formatted.
}
\examples{
top_tables <- list(data.frame(feature_index = 1:10, adj.P.Val = runif(10)))
data <- matrix(runif(100), nrow = 10)
meta <- data.frame(Time = seq(1, 10), batch = rep(c("A", "B"), each = 5))
mode <- "isolated"
spline_params <- list(spline_type = c("n"), dof = list(3))
condition <- "example_condition"
p_values <- runif(10)
clusters <- list("auto")
report_info <- "example_info"
meta_batch_column <- "batch"
time_unit <- "m"
report_dir <- "example_dir"
control_inputs_cluster_hits(top_tables, data, meta, mode, spline_params, 
                            condition, p_values, clusters, report_info, 
                            meta_batch_column, time_unit, report_dir)

}
\seealso{
\code{\link{check_top_tables}}, \code{\link{check_meta}}, 
\code{\link{check_mode}}, \code{\link{check_spline_params}}, 
\code{\link{validate_report_info}}
}
